'use server'

export const signIn = async () =>{
    try {
        // Muttation /database /make a fetch
    } catch (error) {
        console.error('Error', error);
        
    }
}

export const signUp = async (userData: SignUpParams) =>{
    try {
        // create usser account
    } catch (error) {
        console.error('Error', error);
        
    }
}